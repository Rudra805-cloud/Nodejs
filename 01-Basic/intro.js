//Q->what is node js
/*ans=>node js is run time envirement for js its same like for run java we required a javac package same for run js outside of chrome we required node js*/

/*In our chrome or any search engine we have  in chorome we hava engine likee a V8 engine,in safari engine of apple ,and js run on that */

//NODEJS=>(V8 engine ke bahut saare ui related feture dom etc ko nikala aur only un chizo ko rakha hai vo server site pe kaam aate hai ) +(C++)

//NPM =>node package mannager =>do task of install and delete and manage packages 
